fn main()
{
	println!("Hello World!");
	
	let x: i32 = 5; //declare a constant 32-bit integer x to be 5
	let mut y = 10;
	let z = x + y;
	
	y = 20;
	
	let (a, b) = (1, 2);
	
	println!("x = {}", x); //prints the value of x
}